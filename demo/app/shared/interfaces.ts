export interface DemoSources {
  datasource: string;
  template: string;
  styles: string;
}

export interface DemoData {
  title: string;
  titleId: string;
  id: string;
  addClass?: string;
}
