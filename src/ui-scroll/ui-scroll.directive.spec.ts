import { UiScrollDirective } from './ui-scroll.directive';

describe('UiScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new UiScrollDirective();
    expect(directive).toBeTruthy();
  });
});
